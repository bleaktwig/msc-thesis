% --+ SETUP +-------------------------------------------------------------------
\documentclass[aspectratio=169]{beamer}
\usepackage[utf8]{inputenc}
\usepackage[default]{lato}
\usepackage{animate}           % Animated "figures".
\usepackage{booktabs}          % Beautiful tabular.
\usepackage{empheq}            % Equations with emphasis.
\usepackage{hyperref}          % Hyperlinks.
\usepackage{tabularx}          % \textwidth tabular.
\usepackage{textcomp, gensymb} % \degree.

% --+ THEME +-------------------------------------------------------------------
\input{00theme}

% --+ DOCUMENT +----------------------------------------------------------------
\begin{document}
    \colorlet{color_ef}{efd_green}

    % --+ FIRST PAGES +---------------------------------------------------------
    \begin{frame}
        \titlepage
    \end{frame}

    \begin{frame}{Summary}
         This thesis involves three subjects:

        \vspace{12pt}

        \begin{itemize}
            \item
                An \ef{acceptance study} for the RG-E experiment in the CLAS12 detector.

            \vspace{6pt}
            \item
                The development of \ef{alignment software} for the FMT detector.

            \vspace{6pt}
            \item
                The development of the \ef{RG-E target slow controls system}.
        \end{itemize}

        \vspace{12pt}

        This defence will only cover the \ef{acceptance study}, in which we'll explain how we found the $v_z$ region where the RG-E target will be placed.
    \end{frame}

    \begin{frame}{Table of Contents}
        \tableofcontents
    \end{frame}

    % --+ PAGE NUMBER SETUP +---------------------------------------------------
    \pagenumbering{arabic}

    \setbeamertemplate{footline}[text line]{
        \parbox{\linewidth}{\vspace*{-8pt}\ef{\insertsection}\hfill\ef{\insertpagenumber}}
    }

    % Fix page number.
    \addtocounter{framenumber}{-3}

    % --+ CONTEXT +-------------------------------------------------------------
    \graphicspath{{10context/img}}
    \input{10context/00index}

    % --+ DATA ANALYSIS +-------------------------------------------------------
    \graphicspath{{11data_analysis/img}}
    \input{11data_analysis/00index}

    % --+ STUDY RESULTS +-------------------------------------------------------
    \graphicspath{{12study_results/img}}
    \input{12study_results/00index}

    % --+ BACKUP SLIDES +-------------------------------------------------------
    \colorlet{color_ef}{efd_purple}
    \graphicspath{{20backup/img}}
    \input{20backup/00index}
\end{document}
