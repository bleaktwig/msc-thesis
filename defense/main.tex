% --+ SETUP +-------------------------------------------------------------------
\documentclass[aspectratio=169]{beamer}
\usepackage[utf8]{inputenc}
\usepackage[default]{lato}
\usepackage{animate}           % Animated "figures".
\usepackage{booktabs}          % Beautiful tabular.
\usepackage{empheq}            % Equations with emphasis.
\usepackage{hyperref}          % Hyperlinks.
\usepackage{tabularx}          % \textwidth tabular.
\usepackage{textcomp, gensymb} % \degree.

% --+ THEME +-------------------------------------------------------------------
\input{00theme}

% --+ TITLEPAGE SETUP +---------------------------------------------------------
\title{Acceptance Study of the CLAS12 Detector Using the Forward Micromegas Tracker for Run Group E}
\author{Bruno Benkel}
\date{2023-09-14}
% TODO. Add shit and make pretty.

% --+ DOCUMENT +----------------------------------------------------------------
\begin{document}
    \colorlet{color_ef}{efd_green}

    % --+ FIRST PAGES +---------------------------------------------------------
    \begin{frame}[plain]
        \titlepage
    \end{frame}
    % TODO. An summary slide before the TOC would be cool.
    % Mention the objective, the main project of the thesis, + the RG-E slow controls system, + FMT alignment.
    \tableofcontents \pagebreak

    % --+ PAGE NUMBER SETUP +---------------------------------------------------
    \pagenumbering{arabic}

    % Add a number to each frame (but not the total number of frames!)
    \addtobeamertemplate{navigation symbols}{}{
        \usebeamerfont{footline}
        \usebeamercolor[fg]{footline}
        \hspace{1em}
        \insertframenumber
    }

    \addtocounter{framenumber}{-1}
    % --+ CONTEXT +-------------------------------------------------------------
    \graphicspath{{10context/img}}
    \input{10context/00index}

    % --+ DATA ANALYSIS +-------------------------------------------------------
    \graphicspath{{11data_analysis/img}}
    \input{11data_analysis/00index}

    % --+ STUDY RESULTS +-------------------------------------------------------
    \graphicspath{{12study_results/img}}
    \input{12study_results/00index}

    % --+ APPENDICES +----------------------------------------------------------
    \colorlet{color_ef}{efd_purple}
    \graphicspath{{20backup/img}}
    \input{20backup/00index}
\end{document}
